#bug reproduction

```bash
yarn && yarn prod && cd env-test && yarn && yarn start
```

it will show the project working

##in an other console
rebuild in dev mode
```bash
yarn dev
```

you will get the error
